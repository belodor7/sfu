namespace ConsoleApp7;

public enum WheelType
{
    Standard,
    LightAlloy,
    AllTerrain
}

public enum FrameType
{
    Light,
    Medium,
    Heavy
}

public enum CustomerType
{
    Student,
    Weak,
    RichKid,
    Courier,
    AnimeFan,
    Racer
}